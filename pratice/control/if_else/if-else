if-else
코드와 개념을 정리하는 파일

자바와 동일하게 if - else if - else문 작성
if문은 조건식의 값이 true일때 내부의 코드가 실행이 된다
if문은 보통 if - else로 되어있고 상황에 따라
if - else if else 순서대로 사용된다

//if
val test : Int = 10

   //test 변수의 값이 10이 맞을때 true
    if(test == 10){

        println("10 입니다")
    }

    //10이 아닐때 false
    //그러니깐 디버깅창에 결과값이 출력이 되지않는다

    if(test != 10){
        println("10이 아닙니다")
    }

//if - else

//if의 조건식이 참일때 해당 내부 코드를 실행하고
//거짓일 때 else문으로 내려와 해당 내부 코드를 실행
    if(test == 10){
        println("10 입니다")
    }

//if문의 조건거짓일때
    else {
        println("10이 아닙니다")
    }

//if - else if
else if는 먼저 if의 조건식이 true가 아닐 때 다음 조건문으로 else if로 내려와
참인지 거짓인지 판별한다

//if - else if - else
else if문이 true면 해당 구문을 실행시키고 false이면 아무것도 실행하지 않는다
다음으로 else로 내려와 else문의 내부 코드를 실행


//if -else if - else는 조건식이 많을때 사용
//else는 무조건 1개만 사용

if(test == 5){
        println("5입니다")
    }

    else if(test == 10){
        println("10입니다")
    }

    else{
        println("5, 10도 아닙니다")
    }

//단일 함수로 if문을 활용할 수도 있다

 val test6  : Int = 50
    val test7 : Int = 60
    val test_str : String = if (test6 > 50) "50보다 큽니다" else "50보다 작습니다"

    println(test_str)

    val test_str2 : String = if(test6 == 10 && test7 == 60) "true입니다" else "false 입니다"

    println(test_str2)
